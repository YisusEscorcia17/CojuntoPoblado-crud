# Configuración del Servidor
PORT=3000
NODE_ENV=development

# Seguridad - SESSION SECRET
# IMPORTANTE: Cambiar esta clave en producción por una única y segura
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=cambiar-este-secreto-por-uno-generado-aleatoriamente

# Base de Datos PostgreSQL (solo para producción)
# En desarrollo se usa SQLite automáticamente
# DATABASE_URL se obtiene de Render o Supabase
# Ejemplo: postgresql://usuario:password@host:5432/database
DATABASE_URL=

# IMPORTANTE: Las credenciales por defecto se generan automáticamente
# Ver CREDENCIALES_INICIALES.md después del primer inicio
